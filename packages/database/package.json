{
  "name": "database",
  "version": "0.0.0",
  "scripts": {
    "db:generate": "prisma generate",
    "db:push": "prisma db push --skip-generate",
    "build": "tsup"
  },
  "dependencies": {
    "@prisma/client": "latest"
  },
  "devDependencies": {
    "prisma": "latest",
    "tsup": "^8.0.1"
  },
  "main": "./index.ts",
  "types": "./index.ts"
}
